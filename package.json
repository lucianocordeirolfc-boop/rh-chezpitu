{
  "name": "sistema-rh-chez-pitu",
  "private": true,
  "description": "Sistema de Gestão de Pessoal — Grupo Chez Pitu",
  "scripts": {
    "bump-cache": "node scripts/bump-cache.js",
    "test": "node scripts/run-phase2-tests.mjs",
    "test:offline": "node scripts/test-offline-recovery.mjs",
    "test:dedup": "node scripts/test-holiday-deduplication.mjs",
    "test:quota": "node scripts/test-storage-quota.mjs",
    "validate": "node scripts/run-validate.mjs",
    "deploy:preview": "firebase hosting:channel:deploy preview",
    "deploy": "npm run bump-cache && firebase deploy --only hosting",
    "deploy:ci": "firebase deploy --only hosting --non-interactive"
  },
  "devDependencies": {
    "firebase-tools": "^13.0.0"
  }
}
